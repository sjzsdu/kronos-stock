flask==2.3.3
flask-cors==4.0.0
pandas>=2.2.3,<3.0.0
numpy>=1.26.0,<2.0.0
requests>=2.31.0

# PyTorch CPU version (much smaller than default)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0,<2.3.0

einops>=0.6.0
china_stock_data==0.2.1
safetensors>=0.3.0

# Optional (uncomment only if needed):
# plotly==5.17.0              # backend chart generation
# huggingface_hub>=0.34.0,<1.0 # pull models from HF hub
# transformers>=4.35.0,<5.0.0  # only if code uses HuggingFace APIs
# datasets>=2.14.0,<3.0.0      # only for offline dataset prep
